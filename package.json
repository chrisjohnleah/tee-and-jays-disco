{
  "name": "tee-and-jays-disco",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler deploy --config wrangler.worker.jsonc",
    "deploy:worker": "astro build && wrangler deploy --config wrangler.worker.jsonc",
    "cf:dev": "astro build && wrangler dev --config wrangler.worker.jsonc"
  },
  "dependencies": {
    "astro": "^6.3.0"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^13.5.0",
    "wrangler": "^4.89.1"
  }
}
